A minimal JS/HTML/CSS with no libraries if possible.

# Landing/Login Page

A landing page with login and register, login with passkey.

# Logged in page

A kanban board with a dropdown to select the current project.

The board shows all roles in the project as vertical lists, with all the stories assigned
to roles in the list, then marked with their activity (idle, active)

The leftmost list is "unassigned"

Clicking on a story shows a detail view in the panel on the left which inclues the history of the story.

Page contains a dropdown listing all projects.

A javascript kanban board showing the selected project.

A logout button top-right.

A preferences button that takes to the user profile which includes a passkey management.