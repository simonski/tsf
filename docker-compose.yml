version: '3.8'

services:
  initdb:
    build: .
    volumes:
      - ./data:/data
    environment:
      - SF_DB_PATH=/data/sf.db
    command: /app/sf initdb -f /data/sf.db
    restart: "no"

  server:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - SF_DB_PATH=/data/sf.db
    command: /app/sf server -f /data/sf.db -port 8080
    depends_on:
      - initdb
    restart: unless-stopped

  orchestrator:
    build: .
    environment:
      - SF_URL=http://server:8080
      - SF_USERNAME=orchestrator
      - SF_PASSWORD=${ORCHESTRATOR_PASSWORD}
    command: /app/sf orchestrator -url http://server:8080
    depends_on:
      - server
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - server
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:
